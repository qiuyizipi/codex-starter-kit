# 安装说明

1. 在 Codex 中打开“插件”。
2. 添加插件市场仓库：`https://github.com/qiuyizipi/codex-starter-kit`。
3. 新手先安装 `core-pack`；需要代码开发流程时再安装 `dev-pack`。
4. 新建一个 Codex 任务，让新安装的 Skill 被重新发现。

`core-pack` 与 `dev-pack` 均不需要 GitHub Token、Notion 账号或其他凭据。
